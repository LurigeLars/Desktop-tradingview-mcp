# Cloudflare Access application settings.
# Copy to public/gateway.env and keep the real file out of Git.
ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com
ACCESS_AUD=replace-with-access-application-audience
ACCESS_ALLOWED_EMAILS=you@example.com

# Optional hardening / tuning.
# Counts HTTP MCP transport requests, not logical tool calls (~3 requests per tool call).
RATE_PER_MIN=360
MAX_BODY_BYTES=2097152

# Tool exposure. Omit to preserve the full 85-tool surface.
# Optional operator-controlled reduced profile:
# ALLOWED_TOOLS=core
# Explicit full profile (same as default):
# ALLOWED_TOOLS=full
# Or provide an explicit comma-separated allowlist.
